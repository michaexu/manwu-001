# 产品需求文档（PRD）
## 活动领奖微信小程序 —— 客户版 & 商家版

**文档版本：** v1.3  
**创建日期：** 2026-06-03  
**更新日期：** 2026-06-03  
**产品经理：**（待填写）  
**文档状态：** 草稿

> **v1.2 更新说明：** ①活动参与名额支持按会员类型分设配额（VIP 专属名额 + 普通会员名额）；②支持取消报名，取消后积分退还至用户余额。
> **v1.3 更新说明：** ①积分任务页接入微信流量主广告 SDK 自动拉取广告；②新增订阅消息通知功能（报名成功、活动提醒、核销通知）。

---

## 目录

1. [产品概述](#1-产品概述)
2. [问题陈述](#2-问题陈述)
3. [目标与非目标](#3-目标与非目标)
4. [用户角色与画像](#4-用户角色与画像)
5. [功能模块总览](#5-功能模块总览)
6. [用户故事](#6-用户故事)
7. [详细功能需求](#7-详细功能需求)
   - 7.1 [会员注册与登录](#71-会员注册与登录)
   - 7.2 [会员等级与权益](#72-会员等级与权益)
   - 7.3 [积分系统（普通会员）](#73-积分系统普通会员)
   - 7.4 [活动模块（客户端）](#74-活动模块客户端)
   - 7.5 [领奖码（二维码）](#75-领奖码二维码)
   - 7.6 [消息通知（订阅消息）](#76-消息通知订阅消息)
   - 7.7 [商家后台 - 活动管理](#77-商家后台---活动管理)
   - 7.8 [商家后台 - 核销功能](#78-商家后台---核销功能)
   - 7.9 [商家后台 - 会员管理](#79-商家后台---会员管理)
   - 7.10 [管理员功能](#710-管理员功能)
8. [数据模型设计](#8-数据模型设计)
9. [页面流程与交互设计](#9-页面流程与交互设计)
10. [成功指标](#10-成功指标)
11. [非功能性需求](#11-非功能性需求)
12. [开放性问题](#12-开放性问题)
13. [版本规划](#13-版本规划)

---

## 1. 产品概述

本产品是一款微信小程序，分为**客户版**（会员端）和**商家版**（管理端），服务于线下活动奖品领取场景。商家通过小程序发布活动并指定奖品，会员通过参加活动获得领奖资格，到店出示二维码，店员核销后完成领取。

### 1.1 产品定位

| 维度 | 说明 |
|------|------|
| 产品类型 | 微信小程序（**两个独立小程序**：客户版 + 商家版） |
| 核心场景 | 线下活动参与 + 奖品领取核销 |
| 目标用户 | 商家会员体系用户、实体店商家 |
| 商业价值 | 提升线下到店率、增强会员粘性、完成精准营销触达 |

---

## 2. 问题陈述

### 2.1 现状与痛点

**商家侧：**
- 线下活动参与管理混乱，无法准确统计参与人数和核销情况
- 奖品发放依赖人工登记，效率低、易出错、存在重复领取漏洞
- 无法区分高价值会员（VIP）和普通会员，难以精细化运营

**用户侧：**
- 参加活动需要记住各类纸质优惠券或手动登记，体验差
- 普通会员缺乏参与动力，现有积分机制不透明
- 无法方便地查看活动详情、参与状态和领取记录

### 2.2 核心问题

> 如何以低摩擦的方式，实现"活动发布 → 会员参与 → 到店核销"的完整闭环，同时支持 VIP 与普通会员的差异化权益？

---

## 3. 目标与非目标

### 3.1 目标

**用户目标：**
1. 会员可以在 30 秒内完成活动报名
2. 到店出示二维码，店员 10 秒内完成核销
3. 普通会员可通过广告互动获得积分，积分透明可查
4. VIP 会员享受无积分门槛的活动特权

**业务目标：**
1. 减少人工核销错误率至 0
2. 提升线下活动到店转化率
3. 支撑商家对会员的精细化分层运营
4. 通过积分广告产生额外收益来源

### 3.2 非目标（本版本不做）

| 非目标项 | 排除原因 |
|----------|----------|
| 在线商城 / 电商购买流程 | 场景不符，另立产品线 |
| 跨商家积分通兑 | 系统复杂度高，二期规划 |
| 直播 / 视频内容功能 | 与主场景无关 |
| 会员邀请裂变体系 | 需配合运营策略，后续迭代 |
| 商家收款 / 支付功能 | 涉及支付牌照，暂不涉及 |
| 多门店复杂权限管理 | V1 以单商家单门店为主 |

---

## 4. 用户角色与画像

### 4.1 角色总览

```
系统角色
├── 超级管理员（Admin）
│   └── 管理商家、分配 VIP、系统配置
├── 商家操作员（Merchant Operator）
│   ├── 发布/管理活动
│   ├── 核销二维码
│   └── 查看会员参与数据
├── VIP 会员（VIP Member）
│   ├── 无积分限制参与活动
│   └── 由管理员手动分配
└── 普通会员（Regular Member）
    ├── 通过积分参与活动
    └── 通过观看/点击广告获取积分
```

### 4.2 用户画像

**VIP 会员 —— "老周"**
- 45 岁，实体店忠实顾客，消费能力强
- 由商家主动邀请，享受专属服务
- 不需要被积分规则打扰，直接参与活动

**普通会员 —— "小李"**
- 28 岁，价格敏感型用户，愿意为优惠付出一点时间
- 通过点击广告积累积分，兑换活动参与资格
- 希望积分规则清晰，参与流程简单

**商家操作员 —— "王经理"**
- 门店管理者，负责日常运营
- 希望快速发布活动、扫码核销，无需复杂操作
- 需要实时查看活动参与数据

---

## 5. 功能模块总览

### 5.1 客户版（用户端）

| 模块 | 核心功能 |
|------|---------|
| 登录注册 | 微信一键授权登录，自动完成会员注册 |
| 个人中心 | 查看会员类型、积分余额、参与记录 |
| 活动广场 | 浏览活动列表、活动详情 |
| 活动报名 | 普通会员消耗积分报名；VIP 免积分报名 |
| 领奖码 | 展示个人二维码（含活动信息），用于到店核销 |
| 积分任务 | 广告展示 + 积分奖励入口 |

### 5.2 商家版（商家端）

| 模块 | 核心功能 |
|------|---------|
| 活动管理 | 创建/编辑/发布/下架活动 |
| 核销功能 | 扫描会员二维码完成核销 |
| 会员数据 | 查看活动参与会员列表、核销状态 |
| VIP 管理 | 分配/撤销 VIP 权限（管理员） |
| 数据看板 | 活动参与量、核销率统计 |

---

## 6. 用户故事

### 6.1 普通会员

| # | 用户故事 | 优先级 |
|---|----------|--------|
| US-01 | 作为普通会员，我想通过微信一键登录，以便快速进入小程序 | P0 |
| US-02 | 作为普通会员，我想浏览当前所有可参加的活动列表，以便找到感兴趣的活动 | P0 |
| US-03 | 作为普通会员，我想查看活动详情（奖品、时间、地点、所需积分），以便决定是否参加 | P0 |
| US-04 | 作为普通会员，我想消耗积分报名参加一个活动，以便获得到店领取奖品的资格 | P0 |
| US-05 | 作为普通会员，我想在参加活动后看到我的领奖二维码，以便到店出示核销 | P0 |
| US-06 | 作为普通会员，我想通过点击广告获得积分，以便积累参加活动所需的积分 | P0 |
| US-07 | 作为普通会员，我想查看自己的积分余额和获取记录，以便了解积分情况 | P0 |
| US-08 | 作为普通会员，当我积分不足时，我希望系统提示我去赚取积分，以便不错过喜欢的活动 | P1 |
| US-09 | 作为普通会员，我想查看我历史参与过的活动，以便回顾领取记录 | P1 |
| US-10 | 作为普通会员，当同一活动我已经参加过，我希望系统阻止我重复报名并给出提示 | P0 |
| US-10b | 作为会员，我想取消已报名但未核销的活动，并退回消耗的积分 | P0 |

### 6.2 VIP 会员

| # | 用户故事 | 优先级 |
|---|----------|--------|
| US-11 | 作为 VIP 会员，我想无需消耗积分直接报名参加活动，以便享受会员特权 | P0 |
| US-12 | 作为 VIP 会员，我想在个人中心看到 VIP 标识，以便知晓我的会员等级 | P1 |
| US-13 | 作为 VIP 会员，当 VIP 名额已满时，我希望系统提示"VIP 名额已满"，以便了解名额情况 | P1 |

### 6.3 商家操作员

| # | 用户故事 | 优先级 |
|---|----------|--------|
| US-14 | 作为商家操作员，我想创建一个活动并设置奖品名称、领取地点、时间和所需积分，以便向会员发布领奖活动 | P0 |
| US-15 | 作为商家操作员，我想发布/下架一个活动，以便控制活动的可见性 | P0 |
| US-16 | 作为商家操作员，我想用小程序扫描会员的领奖二维码完成核销，以便记录奖品发放 | P0 |
| US-17 | 作为商家操作员，核销时我希望看到该会员的基本信息和活动信息确认，以便避免误核 | P0 |
| US-18 | 作为商家操作员，当二维码已被核销时，系统应阻止重复核销并给出提示 | P0 |
| US-19 | 作为商家操作员，我想查看某活动的参与会员列表，以便了解活动报名情况 | P1 |
| US-20 | 作为商家操作员，我想看到每个会员的核销状态（未核销/已核销），以便管理物资发放 | P1 |
| US-21 | 作为管理员，我想为某个会员开通或取消 VIP 权限，以便灵活管理高价值会员 | P0 |

---

## 7. 详细功能需求

### 7.1 会员注册与登录

#### 功能描述

客户端通过微信 OAuth 授权，自动完成注册和登录，无需填写账号密码。

#### 需求详情

**P0 — 必须实现：**

| 需求ID | 需求描述 | 验收标准 |
|--------|----------|----------|
| REQ-001 | 用户点击"微信授权登录"，系统获取微信 OpenID 和基本信息 | - 成功获取 openid、昵称、头像<br>- 首次登录自动创建会员账号，默认为"普通会员" |
| REQ-002 | 登录后进入首页，会话保持有效期内无需重复登录 | - Token 有效期 ≥ 7 天<br>- 过期后重新授权 |
| REQ-003 | 客户版和商家版为两个独立小程序，分别独立部署和发布 | - 两个 AppID 独立注册<br>- 共享后端 API 和数据库<br>- 客户版面向普通用户公开；商家版仅限白名单人员登录 |

**P1 — 应该实现：**

| 需求ID | 需求描述 | 验收标准 |
|--------|----------|----------|
| REQ-004 | 用户可在个人中心绑定手机号 | - 通过微信手机号快捷绑定<br>- 绑定后手机号作为辅助标识 |

#### 商家版登录鉴权（手机号白名单机制）

| 需求ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| REQ-005 | 商家版登录时，系统通过手机号白名单校验权限 | P0 | - 用户微信授权后获取手机号<br>- 服务端查询白名单表，匹配则允许登录<br>- 不在白名单内：提示"暂无权限，请联系管理员" |
| REQ-006 | 管理员可在后台维护商家操作员手机号白名单 | P0 | - 支持增加/删除手机号<br>- 删除后该手机号立即失去商家端访问权限 |
| REQ-007 | 商家版操作员登录成功后，系统记录其身份用于操作溯源 | P0 | - 核销、活动管理等操作均记录操作员 ID |

#### 边界条件
- 若用户拒绝微信授权，展示提示页并引导用户授权
- 同一微信账号只对应一个会员记录

---

### 7.2 会员等级与权益

#### 功能描述

系统支持两种会员类型：**VIP 会员**（管理员手动分配）和**普通会员**（默认）。

#### 需求详情

| 需求ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| REQ-010 | 新注册会员默认为"普通会员" | P0 | 会员记录中 member_type = 'regular' |
| REQ-011 | 管理员可在后台将指定会员升级为 VIP | P0 | - 操作后会员类型变更为 'vip'<br>- 变更记录可追溯 |
| REQ-012 | 管理员可撤销 VIP 权限，会员恢复为普通会员 | P0 | - 权限实时生效<br>- 用户端 VIP 标识消失 |
| REQ-013 | 用户端展示会员等级标识 | P1 | - 个人中心显示"VIP 会员"或"普通会员"标签<br>- VIP 使用醒目视觉样式 |
| REQ-014 | VIP 会员参加活动时，系统跳过积分校验 | P0 | - 积分余额不足时，VIP 仍可正常报名 |

#### 权益对比

| 权益 | 普通会员 | VIP 会员 |
|------|----------|----------|
| 参加活动 | 需消耗对应积分 | 免费参加（不扣积分） |
| 获取积分 | 通过广告点击 | 不需要（但可有积分） |
| 单活动参加次数 | 1 次 | 1 次 |
| VIP 标识 | ✗ | ✓ |

---

### 7.3 积分系统（普通会员）

#### 功能描述

普通会员通过在客户端观看/点击广告来获取积分，并在参加活动时消耗积分。

#### 需求详情

**积分获取：**

| 需求ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| REQ-020 | 积分任务页通过**微信流量主广告组件**（`<ad>`）展示激励视频广告 | P0 | - 接入微信官方广告 SDK，广告由微信自动拉取填充<br>- 使用 `ad-type="rewarded-video"` 激励视频广告<br>- 用户观看完整视频后获得积分<br>- 管理员在后台配置每个广告位的积分奖励值 |
| REQ-021 | 用户观看完整激励视频后，系统自动发放积分 | P0 | - 发放规则：每个广告位每天限领一次<br>- 服务端校验微信广告回调凭证<br>- 到账后积分余额实时更新 |
| REQ-022 | 防止刷积分：同一广告位同一用户同日限领一次 | P0 | - 超过限制时提示"今日已领取" |
| REQ-023 | 用户可查看积分明细（获取/消耗记录） | P1 | - 显示时间、来源/去向、变动积分数、剩余积分 |

**积分消耗：**

| 需求ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| REQ-024 | 活动报名时，系统自动扣除所需积分 | P0 | - 扣除前显示"将消耗 X 积分"确认弹窗<br>- 扣除后积分余额减少 |
| REQ-025 | 积分不足时，禁止报名并提示去赚取积分 | P0 | - 积分不足弹窗：显示缺口 + 跳转积分任务页按钮 |
| REQ-026 | 用户可取消报名，取消后积分原路退回至余额 | P0 | - 报名成功后，在"我的参与"页可点击"取消报名"<br>- 取消需二次确认弹窗："取消后 X 积分将退回您的账户"<br>- 取消后积分即时退还，有退还记录<br>- 已核销的报名不可取消 |

#### 积分规则配置（后台）

管理员可配置：
- 每个广告的积分奖励值
- 广告每日领取上限
- **积分永久有效，无过期机制，系统不设积分有效期**
- **取消报名后积分原路退还至用户余额，已核销的报名不可取消**

---

### 7.4 活动模块（客户端）

#### 功能描述

客户端核心功能，展示商家发布的活动，支持用户浏览和报名。

#### 活动列表页

| 需求ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| REQ-030 | 展示所有状态为"进行中"的活动 | P0 | - 按发布时间倒序排列<br>- 展示活动封面图、名称、VIP 剩余名额/VIP 总名额、普通剩余名额/普通总名额、截止时间 |
| REQ-031 | 活动列表显示用户参与状态 | P1 | - 已报名的活动显示"已报名"标签<br>- 已满员的显示"已满员"并禁用报名入口 |
| REQ-032 | 活动搜索（可选，V1 暂不做） | P2 | — |

#### 活动详情页

| 需求ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| REQ-033 | 活动详情展示完整信息 | P0 | 包含：活动名称、奖品描述、领取地点、领取时间段、所需积分、VIP 剩余名额/总数、普通会员剩余名额/总数、活动说明 |
| REQ-034 | 普通会员在详情页点击"参加活动"触发积分扣减报名流程 | P0 | - 展示积分余额和所需积分<br>- 余额不足：跳转积分任务<br>- 余额充足：确认扣减后完成报名 |
| REQ-035 | VIP 会员点击"参加活动"直接完成报名（无积分校验） | P0 | - 显示确认弹窗（"即将参加该活动"）<br>- 确认后报名成功 |
| REQ-036 | 同一会员同一活动只能报名一次 | P0 | - 已报名后，按钮变为"查看领奖码"<br>- 接口层也需做幂等校验 |
| REQ-037 | 活动达到对应会员类型的名额上限后，不再接受该类型会员报名 | P0 | - VIP 名额满 → VIP 按钮置灰提示"VIP 名额已满"<br>- 普通名额满 → 普通会员按钮置灰提示"名额已满"<br>- 接口层按会员类型分别做并发控制 |
| REQ-037b | 活动创建时可分别设置 VIP 专属名额和普通会员名额 | P0 | - VIP 专属名额（vip_quota）：VIP 会员可使用的名额数<br>- 普通会员名额（regular_quota）：普通会员可使用的名额数<br>- 两者独立计算，互不占用 |

#### 报名成功页

| 需求ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| REQ-038 | 报名成功后展示确认页 | P0 | - 显示活动名称、领取地点、领取时间<br>- 提供"查看我的领奖码"快捷入口 |
| REQ-039 | 已报名未核销的活动，用户可取消报名 | P0 | - 取消后积分即时退还<br>- 已核销的报名不可取消<br>- 取消后名额释放回对应会员类型池 |

---

### 7.5 领奖码（二维码）

#### 功能描述

每位会员参加活动后，系统生成唯一领奖码（二维码），用于到店出示和核销。

#### 需求详情

| 需求ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| REQ-040 | 报名成功后为会员+活动组合生成唯一二维码 | P0 | - 每个（会员ID + 活动ID）对生成唯一 token<br>- 二维码包含 token 及活动标识 |
| REQ-041 | 用户在"我的参与"页可查看各活动的领奖码 | P0 | - 显示二维码图片<br>- 显示活动名称、领取地点、有效期 |
| REQ-042 | 二维码在被核销前始终有效 | P0 | - V1 不设时间过期（活动结束后可配置是否失效） |
| REQ-043 | 二维码被核销后，页面显示"已核销"状态 | P0 | - 核销后二维码变灰，显示核销时间 |
| REQ-044 | 二维码防伪：包含签名，服务端验证 | P0 | - 使用 HMAC-SHA256 对 token 签名<br>- 防止伪造二维码核销 |

#### 二维码内容设计

```
二维码内容（JSON Base64 编码）：
{
  "type": "claim_code",
  "member_id": "xxx",
  "activity_id": "xxx",
  "registration_id": "xxx",
  "token": "xxx（服务端签名）",
  "generated_at": "timestamp"
}
```

---

### 7.6 消息通知（订阅消息）

#### 功能描述

通过微信**订阅消息**能力，在关键业务节点向用户推送通知，提升用户召回和活动参与率。

#### 前置条件

- 需在微信公众平台申请对应场景的订阅消息模板
- 用户需在小程序内主动授权订阅

#### 需求详情

| 需求ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| REQ-045 | 报名成功通知 | P1 | - 用户报名成功后发送订阅消息<br>- 内容：活动名称、领取地点、领取时间<br>- 引导用户查看领奖码 |
| REQ-046 | 活动即将开始提醒 | P1 | - 活动领取开始前 2 小时发送提醒<br>- 仅通知已报名且未核销的用户<br>- 内容：活动名称、领取地点、开始时间 |
| REQ-047 | 核销成功通知 | P1 | - 奖品核销完成后发送通知<br>- 内容：活动名称、奖品描述、核销时间、核销地点 |
| REQ-048 | 订阅引导：在报名成功页弹出订阅授权弹窗 | P1 | - 一次性请求以上三类消息的订阅授权<br>- 用户拒绝后不再重复弹窗，可在设置中手动开启 |

#### 触发时机汇总

| 事件 | 通知类型 | 接收方 |
|------|---------|--------|
| 活动报名成功 | 报名成功通知 | 报名会员 |
| 活动开始前 2 小时 | 活动提醒 | 已报名未核销会员 |
| 领奖码核销完成 | 核销成功通知 | 被核销会员 |

---

### 7.7 商家后台 - 活动管理

#### 功能描述

商家操作员通过商家版小程序创建和管理活动。

#### 活动创建/编辑

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 活动名称 | 文本 | ✓ | 最多 30 字 |
| 活动封面图 | 图片 | ✓ | 建议尺寸 750×400px |
| 奖品描述 | 富文本 | ✓ | 描述奖品内容 |
| 领取地点 | 文本 | ✓ | 具体门店地址 |
| 领取开始时间 | 时间 | ✓ | 活动开放时间 |
| 领取结束时间 | 时间 | ✓ | 活动截止时间 |
| 参与人数上限 | 数字 | ✓ | 0 = 不限人数（总量，用于展示） |
| VIP 专属名额 | 数字 | ✓ | VIP 会员可用的名额数，0 = 不限 |
| 普通会员名额 | 数字 | ✓ | 普通会员可用的名额数，0 = 不限 |
| 所需积分 | 数字 | ✓ | 0 = 免积分；VIP 始终免费 |
| 活动说明 | 富文本 | - | 注意事项、领取须知等 |
| 活动状态 | 枚举 | - | 草稿/已发布/已下架 |

| 需求ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| REQ-050 | 商家可创建新活动（填写上述字段） | P0 | - 必填字段校验<br>- VIP 名额和普通会员名额分别设置<br>- 保存为草稿或直接发布 |
| REQ-051 | 商家可编辑草稿状态的活动 | P0 | - 已发布活动仅可修改非核心字段（说明、图片）<br>- 不可修改积分数和时间（防止已报名用户受影响） |
| REQ-052 | 商家可发布/下架活动 | P0 | - 发布后用户端可见<br>- 下架后不再显示，已报名用户保留记录 |
| REQ-053 | 活动列表展示所有活动（含各状态） | P0 | - 显示草稿、进行中、已结束、已下架分类 |
| REQ-054 | 活动数据概览：报名人数、核销人数、核销率 | P1 | - 活动卡片上实时展示 |

---

### 7.8 商家后台 - 核销功能

#### 功能描述

商家操作员使用商家版小程序扫描用户出示的领奖二维码，完成奖品发放核销。

#### 核销流程

```
操作员点击"扫码核销"
    → 调起摄像头扫描二维码
        → 解析二维码数据
            → 服务端验证 token 合法性
                ✓ 合法 → 展示核销确认页
                    → 操作员确认
                        → 服务端标记"已核销"
                            → 展示核销成功
                ✗ 非法/已核销/过期 → 展示对应错误提示
```

#### 需求详情

| 需求ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| REQ-060 | 商家版提供"扫码核销"入口，调起微信扫码 | P0 | - 扫码后立即解析并请求服务端验证 |
| REQ-061 | 核销确认页展示会员基本信息和活动信息 | P0 | 显示：头像、昵称、会员类型（VIP/普通）、活动名称、奖品内容 |
| REQ-062 | 确认后标记该领奖记录为"已核销" | P0 | - 核销时间戳记录到数据库<br>- 操作员 ID 记录 |
| REQ-063 | 二维码已核销时，阻止重复核销 | P0 | - 提示"该奖品已于 XX 时间核销"<br>- 不执行重复核销操作 |
| REQ-064 | 二维码内容非法或签名错误时，提示无效 | P0 | - 提示"二维码无效，请联系会员重新获取" |
| REQ-065 | 二维码对应的活动已下架时，提示并询问是否继续核销 | P1 | - 弹窗提示，操作员可强制核销或取消 |
| REQ-066 | 核销记录可在后台查询 | P1 | - 按活动、时间筛选核销记录 |

---

### 7.9 商家后台 - 会员管理

#### 功能描述

商家操作员可查看参与活动的会员信息，管理员可管理 VIP 权限。

#### 需求详情

**会员列表：**

| 需求ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| REQ-070 | 查看所有注册会员列表 | P1 | 显示：头像、昵称、会员类型、注册时间、积分余额 |
| REQ-071 | 按活动筛选参与会员 | P1 | - 选择活动后，显示该活动的报名会员及其核销状态 |
| REQ-072 | 查看单个会员详情 | P1 | 显示：基本信息、参加活动历史、积分记录 |

**VIP 管理（管理员专属）：**

| 需求ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| REQ-073 | 管理员可在会员详情页将会员设为 VIP | P0 | - 操作需二次确认<br>- 变更立即生效 |
| REQ-074 | 管理员可取消会员的 VIP 权限 | P0 | - 操作需二次确认<br>- 撤销后会员恢复为普通会员 |
| REQ-075 | VIP 变更操作记录日志 | P1 | - 记录操作人、操作时间、变更前后状态 |

---

### 7.10 管理员功能

#### 功能描述

超级管理员拥有系统最高权限，负责广告管理、积分规则配置和运营数据查看。

| 需求ID | 需求描述 | 优先级 | 验收标准 |
|--------|----------|--------|----------|
| REQ-080 | 管理员可配置广告位积分奖励规则 | P0 | - 字段：广告位名称、广告位 ID（ad-unit-id）、积分奖励值、每日领取上限、是否启用<br>- 广告内容由微信流量主 SDK 自动填充，无需手动上传 |
| REQ-081 | 管理员可维护商家操作员手机号白名单 | P0 | - 在后台新增/删除手机号<br>- 删除后立即失效，已登录会话下次请求时被拦截 |
| REQ-082 | 数据看板：全局运营数据统计 | P2 | 活动总数、参与总人数、核销总数、积分发放总量 |

---

## 8. 数据模型设计

### 8.1 核心实体

```sql
-- 会员表
CREATE TABLE members (
    id              VARCHAR(36) PRIMARY KEY,
    openid          VARCHAR(64) UNIQUE NOT NULL,    -- 微信 OpenID
    nickname        VARCHAR(50),
    avatar_url      VARCHAR(500),
    phone           VARCHAR(20),
    member_type     ENUM('regular', 'vip') DEFAULT 'regular',
    points_balance  INT DEFAULT 0,                  -- 积分余额
    created_at      DATETIME NOT NULL,
    updated_at      DATETIME NOT NULL
);

-- 活动表
CREATE TABLE activities (
    id              VARCHAR(36) PRIMARY KEY,
    title           VARCHAR(100) NOT NULL,
    cover_image     VARCHAR(500),
    prize_desc      TEXT,                           -- 奖品描述
    location        VARCHAR(200),                   -- 领取地点
    start_time      DATETIME NOT NULL,              -- 领取开始时间
    end_time        DATETIME NOT NULL,              -- 领取结束时间
    max_participants INT DEFAULT 0,                 -- 总参与上限（0=不限）
    vip_quota       INT DEFAULT 0,                 -- VIP 专属名额（0=不限）
    regular_quota   INT DEFAULT 0,                 -- 普通会员名额（0=不限）
    points_required INT DEFAULT 0,                  -- 普通会员所需积分
    description     TEXT,
    status          ENUM('draft','published','unpublished','ended') DEFAULT 'draft',
    current_count   INT DEFAULT 0,                  -- 当前报名人数
    created_by      VARCHAR(36),                    -- 操作员ID
    created_at      DATETIME NOT NULL,
    updated_at      DATETIME NOT NULL
);

-- 活动报名表（含领奖码）
CREATE TABLE registrations (
    id              VARCHAR(36) PRIMARY KEY,
    member_id       VARCHAR(36) NOT NULL,
    activity_id     VARCHAR(36) NOT NULL,
    qr_token        VARCHAR(128) UNIQUE NOT NULL,   -- 领奖码 token（签名）
    points_cost     INT DEFAULT 0,                  -- 实际消耗积分（VIP为0）
    status          ENUM('registered','claimed','cancelled') DEFAULT 'registered',
    claimed_at      DATETIME,                       -- 核销时间
    claimed_by      VARCHAR(36),                    -- 核销操作员ID
    created_at      DATETIME NOT NULL,
    UNIQUE KEY uk_member_activity (member_id, activity_id),  -- 同一会员同一活动仅允许一条有效报名
    cancelled_at    DATETIME,                       -- 取消时间
    INDEX idx_status (status)                       -- 按状态查询索引
);

-- 积分明细表
CREATE TABLE points_records (
    id              VARCHAR(36) PRIMARY KEY,
    member_id       VARCHAR(36) NOT NULL,
    change_type     ENUM('earn_ad','cost_activity','refund_cancel','admin_adjust'),  -- refund_cancel: 取消报名退积分
    change_amount   INT NOT NULL,                   -- 正数=获得，负数=消耗
    balance_after   INT NOT NULL,
    ref_id          VARCHAR(36),                    -- 关联广告ID或活动ID
    remark          VARCHAR(200),
    created_at      DATETIME NOT NULL
);

-- 广告位配置表（微信流量主广告位）
CREATE TABLE ads (
    id              VARCHAR(36) PRIMARY KEY,
    name            VARCHAR(100) NOT NULL,           -- 广告位名称
    ad_unit_id      VARCHAR(100) NOT NULL,           -- 微信流量主广告单元 ID
    points_reward   INT NOT NULL DEFAULT 0,         -- 观看完成后奖励积分
    daily_limit     INT DEFAULT 1,                  -- 每人每日领取上限
    is_active       BOOLEAN DEFAULT TRUE,
    created_at      DATETIME NOT NULL
);

-- 广告领取记录表
CREATE TABLE ad_claim_records (
    id              VARCHAR(36) PRIMARY KEY,
    member_id       VARCHAR(36) NOT NULL,
    ad_id           VARCHAR(36) NOT NULL,
    claimed_date    DATE NOT NULL,                  -- 领取日期（用于每日限制）
    created_at      DATETIME NOT NULL,
    UNIQUE KEY uk_member_ad_date (member_id, ad_id, claimed_date)
);

-- VIP 操作日志表
CREATE TABLE vip_logs (
    id              VARCHAR(36) PRIMARY KEY,
    member_id       VARCHAR(36) NOT NULL,
    action          ENUM('grant','revoke'),
    operator_id     VARCHAR(36),                    -- 操作员ID
    created_at      DATETIME NOT NULL
);
```

### 8.2 关键关联关系

```
members (1) ────────── (N) registrations
activities (1) ──────── (N) registrations
members (1) ────────── (N) points_records
ads (1) ─────────────── (N) ad_claim_records
members (1) ────────── (N) ad_claim_records
```

---

## 9. 页面流程与交互设计

### 9.1 客户端核心流程

#### 普通会员报名流程

```
首页 → 活动广场
    → 点击活动卡片 → 活动详情页
        → 点击"参加活动"
            ├─ VIP 名额已满 → 提示"VIP 名额已满"
            ├─ 普通名额已满 → 提示"名额已满"
            ├─ 积分充足 → 确认扣积分弹窗
            │               → 确认 → 报名成功页
            │                           → "查看领奖码"
            │                               → 领奖码页（展示二维码）
            └─ 积分不足 → 积分不足提示
                            → "去赚积分" → 积分任务页
                                → 点击广告
                                    → 积分到账通知
                                        → 返回活动页再次报名
```

#### 取消报名流程

```
"我的参与" → 选择已报名的活动
    → 点击"取消报名"
        → 确认弹窗："取消后 X 积分将退回您的账户"
            → 确认取消
                → 报名状态变为"已取消"
                → 积分即时退回余额
                → 按钮变为"重新报名"
```

#### VIP 会员报名流程

```
活动详情页 → 点击"参加活动"（免积分）
    → 确认报名弹窗 → 确认 → 报名成功页 → 领奖码
```

#### 到店核销流程

```
会员打开"我的参与" → 选择活动 → 展示二维码
    ↓（同时）
商家操作员打开商家版 → 点击"扫码核销" → 扫描二维码
    → 服务端验证 → 显示确认信息
        → 点击"确认核销" → 核销成功
            → 会员端二维码状态更新为"已核销"
```

### 9.2 商家端核心流程

#### 活动创建与发布流程

```
商家端首页 → 活动管理 → 新建活动
    → 填写活动信息 → 保存草稿 / 直接发布
        → 发布成功 → 活动在客户端可见
```

### 9.3 页面结构（Sitemap）

**客户端（用户版）：**
```
TabBar
├── 首页 / 活动广场
│   └── 活动详情
│       └── 报名成功
├── 积分任务
│   └── 广告详情
├── 我的参与
│   └── 领奖码详情 / 取消报名确认
└── 个人中心
    ├── 积分明细
    └── 设置
```

**商家端（商家版）：**
```
TabBar
├── 活动管理
│   ├── 新建/编辑活动
│   └── 活动详情 & 参与列表
├── 扫码核销
│   └── 核销确认页
├── 会员管理
│   └── 会员详情
└── 数据看板
```

---

## 10. 成功指标

### 10.1 上线 1 个月内（快速指标）

| 指标 | 目标值 | 测量方法 |
|------|--------|----------|
| 会员注册率 | ≥ 60% 活动参与者完成注册 | 注册数 / 活动曝光 UV |
| 活动报名转化率 | ≥ 40% 查看详情的用户完成报名 | 报名数 / 详情页 UV |
| 二维码核销率 | ≥ 70% 已报名会员到店核销 | 核销数 / 报名数 |
| 积分任务完成率 | ≥ 50% 普通会员完成至少一次广告领积分 | 完成人数 / 普通会员总数 |

### 10.2 上线 3 个月内（稳定指标）

| 指标 | 目标值 | 测量方法 |
|------|--------|----------|
| 月活会员数 | 环比增长 20%+ | 月活跃会员 UV |
| 核销错误率 | < 0.1% | 核销异常记录数 / 总核销数 |
| 活动参与人均次数 | ≥ 2 次/月 | 总报名数 / 月活会员数 |
| 商家续约/满意度 | ≥ 80% | 定期调研 |

---

## 11. 非功能性需求

### 11.1 性能要求

| 场景 | 要求 |
|------|------|
| 活动列表加载 | ≤ 1 秒（首屏） |
| 报名接口响应 | ≤ 500ms |
| 二维码生成 | ≤ 200ms |
| 核销接口响应 | ≤ 500ms |
| 高峰并发 | 支持 1000 TPS 核销请求 |

### 11.2 安全要求

| 项目 | 要求 |
|------|------|
| 二维码防伪 | HMAC-SHA256 签名验证 |
| 积分幂等 | 广告领积分接口防重复提交 |
| 报名幂等 | 数据库唯一索引 + 接口锁 |
| 数据传输 | 全程 HTTPS |
| 敏感信息 | 用户 OpenID 不在二维码中明文传输 |

### 11.3 兼容性要求

- 微信基础库版本：≥ 2.10.0
- iOS 13+ / Android 8+
- 小程序包大小：主包 ≤ 1.5MB（可分包）

### 11.4 可用性要求

- 系统可用性 SLA：99.5%
- 核销功能支持离线队列（网络波动时本地缓存，恢复后同步）

---

## 12. 开放性问题

| # | 问题 | 负责方 | 类型 | 优先级 |
|---|------|--------|------|--------|
| ~~Q1~~ | ~~客户版和商家版是否作为两个独立小程序？~~ | — | **已决策：独立双端** | — |
| ~~Q2~~ | ~~商家版登录如何鉴权？~~ | — | **已决策：手机号白名单** | — |
| ~~Q3~~ | ~~积分是否有有效期？~~ | — | **已决策：积分永久有效** | — |
| ~~Q6~~ | ~~活动结束后未核销积分是否退回？~~ | — | **已决策：积分不退回** | — |
| ~~Q4~~ | ~~活动参与人数上限是否支持 VIP 和普通会员分开设定？~~ | — | **已决策：支持分设配额** | — |
| ~~Q5~~ | ~~用户是否支持取消报名？~~ | — | **已决策：支持取消报名，积分退还** | — |
| ~~Q7~~ | ~~广告内容是否接入第三方广告平台？~~ | — | **已决策：接入微信流量主广告 SDK** | — |
| ~~Q8~~ | ~~是否需要消息通知？~~ | — | **已决策：支持微信订阅消息通知** | — |

---

## 13. 版本规划

### V1.0 — MVP（建议 6-8 周）

**目标：** 完成核心闭环，验证用户行为

**包含功能：**
- ✅ 微信登录注册
- ✅ 活动列表 & 详情
- ✅ 普通会员积分报名 + VIP 免积分报名
- ✅ 唯一领奖码生成 & 展示
- ✅ 商家端活动发布 & 管理
- ✅ 商家端扫码核销
- ✅ 商家端手机号白名单鉴权
- ✅ 广告领积分（简单版：固定广告列表，积分永久有效）
- ✅ 管理员 VIP 分配
- ✅ 活动名额按会员类型分设配额（VIP 专属 + 普通）
- ✅ 取消报名 + 积分退还（已核销不可取消）

### V1.1 — 体验优化（V1.0 上线后 2-3 周）

- 积分明细页
- 会员参与历史
- 活动参与人数实时更新
- 核销操作日志
- 取消报名功能（取消后积分退回，已核销不可取消）
- 微信订阅消息通知（报名成功、活动提醒、核销通知）
- 微信流量主广告 SDK 接入（激励视频广告）

### V2.0 — 运营增强（V1.0 上线后 2-3 个月）

- 会员邀请裂变
- 多活动类型（线上/线下）
- 数据看板增强
- 多门店支持

---

## 附录

### A. 名词解释

| 术语 | 解释 |
|------|------|
| 领奖码 | 会员报名活动后生成的唯一二维码，用于到店核销领取奖品 |
| 核销 | 商家操作员扫描领奖码，确认奖品已发放的操作 |
| 积分 | 普通会员通过广告获取的虚拟货币，用于兑换活动参与资格；**永久有效，取消报名后退还** |
| VIP 会员 | 由管理员手动授权的高权益会员，不需要消耗积分参加活动 |
| 普通会员 | 默认会员类型，需消耗积分参加活动 |

### B. 竞品参考

| 产品 | 参考点 |
|------|--------|
| 各品牌会员小程序 | 积分获取与消耗机制 |
| 大众点评核销系统 | 二维码核销 UX 流程 |
| 微信卡券系统 | 防伪机制和核销状态管理 |

---

*文档版本历史：*
| 版本 | 日期 | 修改说明 | 修改人 |
|------|------|----------|--------|
| v1.0 | 2026-06-03 | 初稿创建 | AI 辅助生成 |
| v1.1 | 2026-06-03 | 确认四项产品决策：独立双端、手机号白名单鉴权、积分永久有效、积分消耗不退回 | AI 辅助生成 |
| v1.2 | 2026-06-03 | ①活动名额按会员类型分设配额（VIP 专属 + 普通会员）；②支持取消报名 + 积分退还（已核销不可取消） | AI 辅助生成 |
| v1.3 | 2026-06-03 | ①积分任务接入微信流量主广告 SDK（激励视频）；②新增订阅消息通知（报名成功/活动提醒/核销通知）；所有开放性问题已决策完毕 | AI 辅助生成 |
